<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Portal Attendance Security Group -->
        <record id="group_portal_attendance" model="res.groups">
            <field name="name">Portal Attendance User</field>
            <field name="category_id" ref="base.module_category_human_resources_attendances"/>
            <field name="implied_ids" eval="[(4, ref('base.group_portal'))]"/>
        </record>

        <!-- Record Rule for Portal Users -->
        <record id="portal_attendance_rule" model="ir.rule">
            <field name="name">Portal Attendance: read own attendances</field>
            <field name="model_id" ref="hr_attendance.model_hr_attendance"/>
            <field name="domain_force">[('employee_id.user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>
    </data>
</odoo> 