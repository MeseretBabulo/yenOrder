<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- <record id="view_order_form_inherit_product_hold" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.product.hold</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='order_line']/tree//field[@name='product_id']" position="after">
                <field name="product_id" invisible="1"/>
                <field name="display_type" invisible="1"/>
                <widget name="web_ribbon" title="On Hold" bg_color="bg-danger" decoration-danger="product_id.product_tmpl_id.is_held"/>
            </xpath>
        </field>
    </record> -->
</odoo> 